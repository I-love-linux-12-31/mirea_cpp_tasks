//lesson 4

#include <iostream>


int main() {
    char a[13][64];

    for (int y = 0; y < 13; y++){
        for (int x = 9; x < 64; x++){
            //std::cout << a[y][x];
            if (y < 6 and x < 24) {
                if (y % 2 != 1) {
                    if (x % 2 != 1) {
                        a[y][x] = '*';
                    } else {
                        a[y][x] = ' ';
                    }
                }
                else {
                    if (x % 2 == 1) {
                        a[y][x] = '*';
                    } else {
                        a[y][x] = ' ';
                    }

                }

            }
            else {
                if (x == 24 and y < 6) {
                    a[y][x] = ' ';
                }
                else {
                    a[y][x] = ' ';
                }

            }
        }
    }
    for (int y = 0; y < 13; y++){
        // 0 ; 17
        if (y < 6) {
            a[y][0] = '\033';
            a[y][1] = '[';
            a[y][2] = '0';
            a[y][3] = '0';
            a[y][4] = 'm';
            a[y][5] = '\033';
            a[y][6] = '[';
            a[y][7] = '4';
            a[y][8] = '4';
            a[y][9] = 'm';
        }
        else {
            a[y][0] = '\033';
            a[y][1] = '[';
            a[y][2] = '0';
            a[y][3] = '0';
            a[y][4] = 'm';
            if (y % 2 == 0) {
                a[y][5] = '\033';
                a[y][6] = '[';
                a[y][7] = '4';
                a[y][8] = '1';
                a[y][9] = 'm';
            }
            else {
                a[y][5] = '\033';
                a[y][6] = '[';
                a[y][7] = '4';
                a[y][8] = '7';
                a[y][9] = 'm';
            }

        }

        if (y % 2 == 0) {
            a[y][25] = '\033';
            a[y][26] = '[';
            a[y][27] = '4';
            a[y][28] = '1';
            a[y][29] = 'm';
        }
        else {
            a[y][25] = '\033';
            a[y][26] = '[';
            a[y][27] = '4';
            a[y][28] = '7';
            a[y][29] = 'm';



        }



    }


    for (int y = 0; y < 13; y++){
        for (int x = 0; x < 64; x++){
            std::cout << a[y][x];
        }
        std::cout << '\033' << '[' << '0' << 'm' << std::endl;
    }

    return 0;
}

